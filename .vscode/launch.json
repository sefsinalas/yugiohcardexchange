{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Local Server",
            "type": "python",
            "request": "launch",
            "module": "http.server",
            "args": ["8000"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
